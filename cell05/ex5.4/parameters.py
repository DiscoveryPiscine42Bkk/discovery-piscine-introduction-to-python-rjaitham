# parameters.py
import sys

# Calculate the number of parameters (excluding the script name)
num_params = len(sys.argv) - 1

# Display the number of parameters
print(f"Number of parameters: {num_params}.")